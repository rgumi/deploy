matrix:
  include:
    - language: node_js
      node_js:
        - "node"
      before_script:
        - cd vue
      script:
        - npm install
        - npm run build

    - language: go
      go:
        - "1.15"
      before_script:
        - cd src
      scrpt:
        - export GO111MODULE=on
        - go get -t -v ./...
        - go test -v ./...
        - go build
